# 电子沙漏 (Hourglass) 产品设计文档

> 硬件平台：NMTech Display 2.8 (ESP32-S3 + ST7789 320×240 + QMI8658 + ES8311)  
> 分支：Hourglass

---

## 1. 产品概述

将 2.8 英寸横屏设备竖向握持，屏幕以软件渲染的方式模拟一个真实沙漏：  
- IMU 实时获取设备姿态，计算重力方向；  
- 物理引擎驱动沙粒（或其他填充物）在两个腔体间流动；  
- 沙粒全部落入下腔后触发计时完成提示音；  
- 翻转设备即可重置沙漏，开始下一轮计时；  
- 通过 Wi-Fi Web 后台配置计时时长、填充物模式、音效等。

---

## 2. 系统架构

```
┌────────────────────────────────────────────────────┐
│                  FreeRTOS Tasks                     │
│                                                     │
│  IMU Task         Physics Task        LVGL Task     │
│  (50 Hz)    ───▶  (30 Hz)      ───▶  (30 fps)      │
│  QMI8658          粒子系统             canvas渲染    │
│  Mahony AHRS      碰撞/流动            hourglass UI  │
│      │                │                             │
│      └────gravity─────┘                             │
│                                                     │
│  Audio Task       Web Task                          │
│  ES8311           HTTP Server / WebSocket           │
│  提示音/流沙音     参数配置                           │
└────────────────────────────────────────────────────┘
```

**核心数据流：**
```
QMI8658 原始数据
    → Mahony AHRS (已有实现)
        → Euler 角 (roll / pitch)
            → 屏幕坐标系重力向量 (gx, gy)
                → 物理引擎输入
                    → 粒子位置更新
                        → LVGL Canvas 渲染
```

---

## 3. 屏幕布局与页面导航

屏幕逻辑分辨率 320×240。当前 UI 使用 LVGL `tileview` 做横向分页，页面从左到右排列：

- 第 1 页：AHRS / IMU 调试仪表盘，保留用于姿态、欧拉角和坐标系验证。
- 第 2 页：沙漏主页面，承载后续 M2 物理引擎和 M4 Canvas 渲染的完整展示。
- 第 3 页及后续：预留给设置、音频、Web 配置状态等扩展功能。

交互约定：

- 从 AHRS 页右滑进入沙漏页。
- 从沙漏页左滑返回 AHRS 页。
- 后续新增功能继续向右追加 tile，不破坏已有页面。

### 3.1 AHRS 调试页

AHRS 调试页用于开发期验证 IMU：

- 页面进入时触发 5 秒 gyro bias 校准。
- 校准期间显示半透明遮罩和倒计时，提示设备保持静止。
- 校准完成后显示人工地平仪风格的姿态仪表盘。
- 仪表左上角显示校准后的 Euler 原始数值：Roll / Pitch / Yaw。

### 3.2 沙漏主页面

沙漏页面是产品主体验。M1 阶段先显示占位画面，M2 起替换为真实物理模拟和渲染。

```
┌──────────────────────┐  ← 240px
│   [状态栏: 时间/模式] │  ← 24px
├──────────────────────┤
│    ╱‾‾‾‾‾‾‾‾‾‾‾╲    │
│   ╱  上 腔 体    ╲   │
│  │  ● ● ● ● ●    │  │  ← 上腔：沙粒从此流出
│  │   ● ● ● ●     │  │
│   ╲___________╱   │
│         │          │  ← 细颈：控制流量
│    ╱‾‾‾‾‾‾‾‾‾‾‾╲   │
│   ╱               ╲  │
│  │                 │  │  ← 下腔：沙粒堆积处
│   ╲___________╱   │
├──────────────────────┤
│  [进度条 / 剩余时间]  │  ← 24px
└──────────────────────┘
```

- 沙漏外轮廓：预渲染矢量多边形（两个等腰三角形，顶角相接）  
- 渲染顺序：背景 → 沙漏外壳 → 粒子层 → UI 覆盖层  
- 设备翻转时屏幕内容同步旋转 180°（LVGL `lv_display_set_rotation`）

---

## 4. IMU 姿态解算

### 4.1 现有基础
- 驱动：QMI8658（I2C 0x6B），已有 HAL 接口  
- 算法：`MahonyAHRS`（已实现，输出四元数 / 欧拉角）  
- 偏置校准：`ImuBias`（已实现）

### 4.2 重力向量映射
从 Mahony 输出的 `roll`（绕 X 轴）和 `pitch`（绕 Y 轴）计算屏幕平面内的重力方向向量：

```
gx_screen =  sin(roll)
gy_screen =  sin(pitch) * cos(roll)   // 归一化后使用
```

重力向量决定：
- 粒子受力方向  
- 沙粒"堆积面"的倾斜角度  
- 设备翻转检测（gy_screen 符号反转 → 触发沙漏翻转逻辑）

### 4.3 翻转检测
当 `pitch` 超过 ±150° 时判定为翻转，启动翻转动画并重新分配粒子到上腔。  
加入 300 ms 防抖，避免晃动误触发。

---

## 5. 物理引擎

### 5.1 总体策略

采用**基于网格的粒子系统**（Grid-Accelerated Particle System）：

- 每个粒子存储浮点坐标 `(x, y)`、速度 `(vx, vy)`；  
- 以像素网格（4×4 px/cell）做宽相碰撞检测，降低 O(N²) 到 O(N)；  
- 沙漏边界用预先计算的像素掩码（bitmap）表示，粒子碰到掩码边界时反弹/停止；  
- 细颈区域设置"流量令牌"，每帧最多允许 K 个粒子通过，模拟颈部阻力。

### 5.2 粒子数量估算

| 填充模式 | 粒子数 | 粒子渲染半径 |
|----------|--------|-------------|
| 细沙     | 400~600 | 2 px       |
| 粗沙/石子 | 80~150 | 4~6 px     |
| 液态水   | 300~500 | 2 px（连续渲染）|

ESP32-S3 @ 240 MHz，单核 30 Hz 物理步进，500 粒子预计 CPU < 40%。

### 5.3 每帧物理步骤

```
1. 读取重力向量 (gx, gy)，标准化
2. 对每个粒子：
   a. 加速度 = gravity_scale × (gx, gy)
   b. 速度更新：v += a × dt，乘以阻尼系数 damping
   c. 位置更新：pos += v × dt
   d. 边界检测（沙漏掩码）：
      - 命中壁面 → 速度分量反射，乘以弹性系数 restitution
      - 命中细颈出口 → 判断流量令牌
   e. 粒子间碰撞（网格宽相 + 圆形窄相）：
      - 两粒子重叠 → 分离冲量（弹性 or 非弹性）
3. 细颈流量控制：本帧已通过粒子数 ≥ K → 关闭细颈（剩余粒子停在颈部）
4. 统计下腔粒子数，判断计时完成
```

### 5.4 不同填充物的参数差异

| 参数 | 细沙 | 粗沙/石子 | 液态水 |
|------|------|-----------|--------|
| `gravity_scale` | 200 | 150 | 180 |
| `damping` | 0.85 | 0.80 | 0.95（高粘） |
| `restitution`（壁面弹性）| 0.1 | 0.3 | 0.05 |
| `restitution`（粒子间）| 0.05 | 0.4 | 0.02 |
| 细颈每帧流量 K | 3~5 | 1~2 | 5~8（流速快）|
| 特殊效果 | - | 碰撞声 | 表面张力聚合 |

### 5.5 液态水特殊处理

水不表现为独立粒子，而是进行**连通域合并渲染**：
- 相邻粒子距离 < 阈值时，渲染连接桥（椭圆填充），视觉上呈水滴/水面；  
- 用 `marching squares` 轻量变体提取轮廓，勾勒水面高光线；  
- 水面"晃动"：在重力方向改变时，增加横向速度扰动。

---

## 6. 渲染方案

### 6.1 LVGL Canvas

使用一个全屏 `lv_canvas`（PSRAM 分配），Physics Task 写入粒子位置，LVGL Task 提交渲染。  
双缓冲策略：Physics Task 写 Buffer A，LVGL Task 读 Buffer B，每帧交换。

### 6.2 渲染层次

```
Layer 0: 背景（纯色或渐变）
Layer 1: 沙漏外壳（预绘制位图 or LVGL polygon）
Layer 2: 粒子层（canvas 逐粒子绘制圆形 or 预生成粒子精灵）
Layer 3: 沙漏玻璃高光（半透明白色折射效果，固定位图叠加）
Layer 4: UI 覆盖（状态栏、进度条、图标）
```

### 6.3 性能优化

- 粒子精灵预生成为 palette 颜色块，直接 `memcpy` 到 canvas 帧缓冲；  
- 只重绘粒子活动区域（dirty rect），减少 SPI 传输量；  
- 沙漏外壳静态部分使用 `lv_img` 缓存，不随帧重绘。

---

## 7. 计时逻辑

```
状态机：
  IDLE ──[翻转]──▶ RUNNING ──[沙粒耗尽]──▶ DONE
   ▲                                          │
   └──────────────[翻转]─────────────────────┘

RUNNING：
  - 记录开始时刻 (RTC / millis)
  - 持续统计下腔粒子数
  - 下腔粒子数 == 总粒子数 → 进入 DONE

DONE：
  - 播放完成音效（ES8311）
  - 状态栏显示"完成"动画
  - 等待翻转 → 回到 IDLE/RUNNING
```

**计时精度：** 粒子全部落下的时刻即为计时结束，与配置时长的误差由细颈流量 K 和粒子数共同决定，可通过参数标定使误差 < 2%。

---

## 8. 音效方案（ES8311）

| 事件 | 音效 | 描述 |
|------|------|------|
| 翻转启动 | `flip.wav` | 短促沙沙声 (~0.5s) |
| 沙粒流动中 | `sand_loop.wav` | 循环环境音，音量随流速变化 |
| 计时完成 | `done.wav` | 清脆提示音 (~1s) |
| 石子碰撞 | `pebble.wav` | 随机触发，粒子撞壁时播放 |
| 水流动 | `water_loop.wav` | 轻柔流水声 |

- 音频文件存储在 SPIFFS（小文件）或 SD 卡；  
- 使用 ES8311 I2S 播放，支持混音（流动声 + 完成音叠加）；  
- 音量可由 Web 后台配置。

---

## 9. Web 配置后台

### 9.1 Wi-Fi 模式

- 首次上电（或长按 BOOT 键）：启动 **AP 模式**，SSID = `Hourglass-XXXX`；  
- 配置完 STA 后自动切换 STA 模式，后续在局域网访问；  
- IP 地址显示在状态栏。

### 9.2 配置项

```json
{
  "timer_minutes": 5,
  "material": "fine_sand",      // fine_sand | coarse_sand | pebble | water
  "sound_enable": true,
  "volume": 70,
  "brightness": 80,
  "flip_sensitivity": "normal", // low | normal | high
  "wifi_ssid": "...",
  "wifi_password": "..."
}
```

### 9.3 接口设计

| 路径 | 方法 | 描述 |
|------|------|------|
| `/` | GET | 配置页面（单页 HTML，内联 CSS/JS）|
| `/api/config` | GET | 读取当前配置（JSON）|
| `/api/config` | POST | 保存配置并重启物理引擎 |
| `/api/status` | GET | 当前状态（运行时间、粒子数、翻转次数）|
| `/api/restart` | POST | 重启设备 |

- 使用 ESP32 Arduino `WebServer` 库；  
- 配置持久化到 NVS（非易失存储）；  
- Web UI 资源打包进 SPIFFS。

---

## 10. 功能模块划分

```
src/
├── app/
│   ├── application.cpp/h     (已有，扩展 begin() 启动各任务)
│   └── task_config.h         (扩展任务优先级/栈大小)
├── physics/
│   ├── particle.h            (粒子数据结构)
│   ├── hourglass_sim.cpp/h   (物理引擎主体)
│   └── material_profile.h   (各材质参数表)
├── ui/
│   ├── hourglass_ui.cpp/h    (LVGL 渲染、沙漏外壳绘制)
│   └── status_bar.cpp/h     (状态栏、进度条)
├── imu/
│   └── imu_task.cpp/h        (IMU 采样任务，输出重力向量)
├── audio/
│   └── audio_manager.cpp/h   (ES8311 音效管理)
├── web/
│   ├── web_server.cpp/h      (HTTP Server)
│   └── config_store.cpp/h   (NVS 配置读写)
└── alg/                      (已有 Mahony AHRS，保留)
```

---

## 11. 关键风险与待定事项

| 风险点 | 严重性 | 应对措施 |
|--------|--------|----------|
| 500 粒子物理计算帧率不足 | 高 | 先用 200 粒子验证，用 ESP32-S3 双核（Core1 跑物理）|
| 液态水表面渲染开销大 | 中 | 降级为简单连通块，不做 marching squares |
| 细颈流量标定使计时误差 < 2% | 中 | 实测后通过查找表映射时长→K 值 |
| Wi-Fi 与 I2S 同时运行的干扰 | 低 | 已知 ESP32 Wi-Fi 与 I2S 共存，需测试噪声 |
| IMU 陀螺仪漂移影响翻转判定 | 低 | 以加速度为主，陀螺仪仅做补偿，不依赖 yaw |

---

## 12. 开发里程碑（草稿）

| 阶段 | 目标 |
|------|------|
| M1 | IMU 姿态采样 → 重力向量输出，屏幕显示调试数值 |
| M2 | 物理引擎基础版：细沙粒子在沙漏边界内自由落体 |
| M3 | 完整沙漏流动：细颈流量控制 + 翻转检测 + 计时状态机 |
| M4 | LVGL 渲染完善 + 状态栏 + 进度条 |
| M5 | 音效接入（ES8311）|
| M6 | Web 配置后台 |
| M7 | 多材质模式（粗沙/石子/水）|
| M8 | 性能调优 + 外观打磨 |
